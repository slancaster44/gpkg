--- Gpkg Specification --
	-- compile.sh
	-- postfake.sh
	-- postinstall.sh
	-- pkginfo.json
		-- Name
		-- Version
		-- Description
		-- Install Options
			DESTDIR not valid option
		-- Dependencies
	-- tarball

-- Installation Process
	-- untar tarball in tmp dir
	-- compile.sh
	-- set fakeroot
	-- run install
	-- run postfake.sh
	-- map files, and save file listings 
	-- save copy of pkginfo
	-- relocate package OR create symlinks to all?
	-- postinstall.sh


TODO:
	Add -Am makes association with file and program
	Add -Ar removes association with file and program

	Create Build tool for building .gpkg files from .tar.xz files

	Add Repository support
		1) Find programs in repository
			A) Add/rm repo
			B) Search repos
		2) Install programs from repository
	
	Dependency Resolution
		1) Find dependencies in repository
		2) Mapp proper install order of depndencies
		3) Install dependencies & program

	Dependency Removal
		1) Find orphaned dependencies
		2) Remove orphaned dependencies

	Upgrade Package from Repository

	Rebrand from GPKG to DMI #Dandified Make Install